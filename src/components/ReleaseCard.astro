---
import { Image } from "astro:assets";

import { formatArticleDate } from "../lib/article-dates";
import { formatReleaseStargazerCount, getReleaseRepoName, type ReleaseActivity } from "../lib/articles";

type Props = {
  release: ReleaseActivity;
};

const { release } = Astro.props;
const repoName = getReleaseRepoName(release.repo);
const stargazerCount = formatReleaseStargazerCount(release.stargazerCount);
---

<article class="release-card activity-card activity-card--compact">
  <a href={release.url} class="release-card__cover-link activity-card__cover-link" target="_blank" rel="noreferrer">
    <Image
      src={release.coverUrl}
      alt={release.coverAlt}
      width={480}
      height={252}
      sizes="(max-width: 720px) 100vw, 20rem"
    />
  </a>
  <div class="release-card__body activity-card__body">
    <h3>
      <a href={release.url} class="activity-card__title-link" target="_blank" rel="noreferrer">{repoName}</a>
    </h3>
    <div class="release-card__copy">
      {release.description && <p class="release-card__description">{release.description}</p>}
    </div>
    <div class="release-card__footer">
      <p class="release-card__meta activity-card__meta">
        <span class="release-card__meta-date">
          <span class="release-card__meta-label">最新リリース日</span>
          <span>{formatArticleDate(new Date(release.publishedAt))}</span>
        </span>
      </p>
      <div class="release-card__metrics">
        {release.license && <p class="release-card__license">{release.license}</p>}
        <p class="release-card__version">{release.version}</p>
        <span class="release-card__stars" aria-label={`GitHub stars ${stargazerCount}`}>
          <svg class="release-card__star-icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
            <path d="M12 3.25 14.703 8.728l6.047.879-4.375 4.265 1.033 6.022L12 17.051l-5.408 2.843 1.033-6.022-4.375-4.265 6.047-.879L12 3.25Z" />
          </svg>
          <span class="release-card__star-count">{stargazerCount}</span>
        </span>
      </div>
    </div>
  </div>
</article>

<style>
  .release-card__cover-link {
    aspect-ratio: 1200 / 630;
  }

  .release-card__cover-link img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .release-card__body {
    gap: 0.4rem;
  }

  .release-card__copy,
  .release-card__description,
  .release-card__meta,
  .release-card__meta-label,
  .release-card__license,
  .release-card__version,
  .release-card__stars {
    margin: 0;
  }

  .release-card h3 {
    margin: 0;
    font-size: clamp(0.96rem, 0.9rem + 0.28vw, 1.08rem);
    line-height: 1.32;
    display: -webkit-box;
    overflow: hidden;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
  }

  .release-card__copy {
    min-height: 0;
  }

  .release-card__meta-date {
    display: inline-flex;
    gap: 0.35rem;
    flex-wrap: wrap;
  }

  .release-card__description {
    color: var(--muted-color);
    font-size: 0.92rem;
    display: -webkit-box;
    overflow: hidden;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
  }

  .release-card__footer {
    display: grid;
    gap: 0.55rem;
    align-content: end;
    margin-top: auto;
  }

  .release-card__metrics {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    align-items: center;
  }

  .release-card__meta {
    font-size: 0.84rem;
  }

  .release-card__version {
    width: fit-content;
    padding: 0.3rem 0.55rem;
    border-radius: 999px;
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 500;
  }

  .release-card__license {
    width: fit-content;
    padding: 0.3rem 0.55rem;
    border: 1px solid rgba(30, 27, 24, 0.12);
    border-radius: 999px;
    background: rgba(30, 27, 24, 0.04);
    color: var(--muted-color);
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 500;
  }

  .release-card__version {
    background: rgba(14, 107, 99, 0.1);
    color: var(--accent-color);
  }

  .release-card__stars {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    width: fit-content;
    padding: 0.3rem 0.55rem;
    border: 1px solid rgba(14, 107, 99, 0.16);
    border-radius: 999px;
    color: var(--text-color);
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 500;
  }

  .release-card__star-icon {
    flex: none;
    width: 0.85rem;
    height: 0.85rem;
    fill: #a86a00;
    opacity: 1;
  }

  .release-card__star-count {
    line-height: 1;
  }
</style>
