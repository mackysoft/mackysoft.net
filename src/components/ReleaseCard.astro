---
import { Image } from "astro:assets";

import { getUiText } from "../lib/copy";
import type { SiteLocale } from "../lib/i18n";
import { formatArticleDate } from "../lib/article-dates";
import { formatReleaseStargazerCount, getReleaseRepoName, type ReleaseActivity } from "../lib/releases";

type Props = {
  release: ReleaseActivity;
  locale?: SiteLocale;
};

const { release, locale = "ja" } = Astro.props as Props;
const repoName = getReleaseRepoName(release.repo);
const stargazerCount = formatReleaseStargazerCount(release.stargazerCount, locale);
const uiText = getUiText(locale);
---

<article class="release-card activity-card activity-card--compact">
  <a href={release.url} class="activity-card__link-layer" aria-label={repoName} target="_blank" rel="noreferrer"></a>
  <div class="release-card__cover activity-card__cover">
    <Image
      src={release.coverUrl}
      alt={release.coverAlt}
      width={480}
      height={252}
      sizes="(max-width: 720px) 100vw, 20rem"
    />
  </div>
  <div class="release-card__body activity-card__body">
    <h3 class="activity-card__title">{repoName}</h3>
    <div class="release-card__copy">
      {release.description && <p class="release-card__description">{release.description}</p>}
    </div>
    <div class="release-card__footer">
      <p class="release-card__meta activity-card__meta">
        <span class="release-card__meta-date">
          <span class="release-card__meta-label">{uiText.release.latestReleaseDate}</span>
          <span>{formatArticleDate(new Date(release.publishedAt), locale)}</span>
        </span>
      </p>
      <div class="release-card__metrics">
        {release.license && <p class="release-card__license">{release.license}</p>}
        <p class="release-card__version">{release.version}</p>
        <span class="release-card__stars" aria-label={`GitHub stars ${stargazerCount}`}>
          <svg class="release-card__star-icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
            <path d="M12 3.25 14.703 8.728l6.047.879-4.375 4.265 1.033 6.022L12 17.051l-5.408 2.843 1.033-6.022-4.375-4.265 6.047-.879L12 3.25Z" />
          </svg>
          <span class="release-card__star-count">{stargazerCount}</span>
        </span>
      </div>
    </div>
  </div>
</article>

<style>
  .release-card__cover {
    aspect-ratio: 1200 / 630;
  }

  .release-card__cover img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .release-card__body {
    gap: 0.4rem;
  }

  .release-card__copy,
  .release-card__description,
  .release-card__meta,
  .release-card__meta-label,
  .release-card__license,
  .release-card__version,
  .release-card__stars {
    margin: 0;
  }

  .release-card h3 {
    margin: 0;
    font-size: clamp(0.96rem, 0.9rem + 0.28vw, 1.08rem);
    line-height: 1.32;
    display: -webkit-box;
    overflow: hidden;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
  }

  .release-card__copy {
    min-height: 0;
  }

  .release-card__meta-date {
    display: inline-flex;
    gap: 0.35rem;
    flex-wrap: wrap;
  }

  .release-card__description {
    color: var(--muted-color);
    font-size: 0.92rem;
    display: -webkit-box;
    overflow: hidden;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
  }

  .release-card__footer {
    display: grid;
    gap: 0.55rem;
    align-content: end;
    margin-top: auto;
  }

  .release-card__metrics {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    align-items: center;
  }

  .release-card__meta {
    font-size: 0.84rem;
  }

  .release-card__version {
    width: fit-content;
    padding: 0.3rem 0.55rem;
    border-radius: 999px;
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 500;
  }

  .release-card__license {
    width: fit-content;
    padding: 0.3rem 0.55rem;
    border: 1px solid var(--surface-soft-border-color);
    border-radius: 999px;
    background: var(--surface-soft-color);
    color: var(--muted-color);
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 500;
  }

  .release-card__version {
    background: var(--accent-surface-color);
    color: var(--accent-color);
  }

  .release-card__stars {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    width: fit-content;
    padding: 0.3rem 0.55rem;
    border: 1px solid var(--accent-border-color);
    border-radius: 999px;
    color: var(--text-color);
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 500;
  }

  .release-card__star-icon {
    flex: none;
    width: 0.85rem;
    height: 0.85rem;
    fill: var(--star-color);
    opacity: 1;
  }

  .release-card__star-count {
    line-height: 1;
  }
</style>
