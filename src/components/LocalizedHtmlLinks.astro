---
import { defaultLocale, type SiteLocale } from "../lib/i18n";
import { localizeHtmlLinks } from "../lib/localized-html-links";

type Props = {
  locale?: SiteLocale;
};

const { locale = defaultLocale } = Astro.props as Props;
const localizedHtml = locale === defaultLocale ? null : localizeHtmlLinks(await Astro.slots.render("default"), locale);
---

{localizedHtml ? <Fragment set:html={localizedHtml} /> : <slot />}
