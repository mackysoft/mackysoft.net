---
import { externalLinks, primaryNav, siteMeta } from "../lib/site";

type Props = {
  title?: string;
  description?: string;
};

const { title, description = siteMeta.description } = Astro.props;
const pageTitle = title ? `${title} | ${siteMeta.name}` : siteMeta.name;
const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700;900&family=Roboto+Mono:wght@400;500;700&display=swap"
    />
  </head>
  <body>
    <div class="shell">
      <header class="site-header">
        <a href="/" class="site-brand">{siteMeta.name}</a>
        <nav aria-label="Global">
          <ul class="site-nav">
            {primaryNav.map((item) => (
              <li>
                <a href={item.href} aria-current={currentPath === item.href ? "page" : undefined}>
                  {item.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      </header>
      <main class="site-main">
        <slot />
      </main>
      <footer class="site-footer">
        <ul class="footer-links">
          {externalLinks.map((link) => (
            <li><a href={link.href} target="_blank" rel="noreferrer">{link.label}</a></li>
          ))}
        </ul>
      </footer>
    </div>
  </body>
</html>

<style is:global>
  :root {
    --bg-color: #f5f1e8;
    --surface-color: rgba(255, 252, 246, 0.78);
    --surface-strong-color: #ece5d8;
    --text-color: #1e1b18;
    --muted-color: #635b52;
    --accent-color: #0e6b63;
    --border-color: rgba(30, 27, 24, 0.16);
    --code-bg-color: #181512;
    --code-text-color: #f5efe2;
    --font-body: "Roboto", "Helvetica Neue", Arial, sans-serif;
    --font-mono: "Roboto Mono", "SFMono-Regular", Consolas, monospace;
  }

  * {
    box-sizing: border-box;
  }

  html {
    background:
      radial-gradient(circle at top left, rgba(14, 107, 99, 0.14), transparent 28rem),
      linear-gradient(180deg, #f7f4ed 0%, #f2ede3 100%);
    color: var(--text-color);
    font-family: var(--font-body);
    line-height: 1.68;
  }

  body {
    margin: 0;
  }

  a {
    color: var(--accent-color);
  }

  img {
    max-width: 100%;
    height: auto;
  }

  code,
  pre {
    font-family: var(--font-mono);
  }

  pre {
    overflow-x: auto;
    padding: 1rem 1.1rem;
    border-radius: 1rem;
    background: var(--code-bg-color);
    color: var(--code-text-color);
  }

  table {
    width: 100%;
    border-collapse: collapse;
  }

  th,
  td {
    padding: 0.75rem;
    border: 1px solid var(--border-color);
    text-align: left;
  }

  blockquote {
    margin: 0;
    padding: 0.9rem 1rem;
    border-left: 3px solid var(--accent-color);
    background: rgba(14, 107, 99, 0.06);
  }

  .shell {
    width: min(72rem, calc(100vw - 2rem));
    margin: 0 auto;
    padding: 1rem 0 4rem;
  }

  .site-header {
    display: grid;
    gap: 1rem;
    padding: 1rem 0 1.5rem;
  }

  .site-brand {
    font-size: clamp(1.4rem, 1.2rem + 0.8vw, 1.9rem);
    font-weight: 700;
    letter-spacing: 0.02em;
    color: var(--text-color);
    text-decoration: none;
  }

  h1,
  h2,
  h3,
  .site-brand,
  .site-nav a,
  .button-link {
    font-family: var(--font-body);
  }

  .eyebrow {
    margin: 0 0 0.4rem;
    color: var(--accent-color);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-size: 0.82rem;
    font-family: var(--font-mono);
    font-weight: 500;
  }

  .site-nav,
  .footer-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.65rem 0.9rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .site-nav a,
  .footer-links a {
    display: inline-flex;
    padding: 0.2rem 0;
    color: var(--muted-color);
    text-decoration: none;
    border-bottom: 1px solid transparent;
  }

  .site-nav a[aria-current="page"] {
    color: var(--text-color);
    border-color: var(--text-color);
  }

  .site-main {
    display: grid;
    gap: 2rem;
  }

  .site-footer {
    display: grid;
    gap: 0.75rem;
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border-color);
    color: var(--muted-color);
  }

  .content-panel {
    padding: 1.6rem;
    border: 1px solid var(--border-color);
    border-radius: 1.4rem;
    background: var(--surface-color);
    backdrop-filter: blur(12px);
  }

  .content-panel h1,
  .content-panel h2,
  .content-panel h3 {
    line-height: 1.2;
  }

  .content-grid {
    display: grid;
    gap: var(--content-grid-gap, 1rem);
  }

  .content-grid > :where(p, ul, ol) {
    margin: 0;
  }

  .content-grid > :last-child {
    margin-bottom: 0;
  }

  .activity-card {
    display: grid;
    border: 1px solid var(--border-color);
    border-radius: 1.25rem;
    background: var(--surface-color);
    overflow: hidden;
  }

  .activity-card--compact {
    grid-template-rows: auto minmax(0, 1fr);
    gap: 0.75rem;
    min-height: 19.75rem;
    padding: 1rem;
  }

  .activity-card__cover-link {
    display: block;
    overflow: hidden;
    border-radius: 0.9rem;
    background: var(--surface-strong-color);
  }

  .activity-card__cover-link img {
    display: block;
    width: 100%;
    height: auto;
  }

  .activity-card__body {
    display: grid;
    grid-template-rows: auto minmax(0, 1fr) auto;
    min-height: 0;
  }

  .activity-card__title-link {
    color: var(--text-color);
    text-decoration: none;
  }

  .activity-card__meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin: 0;
    color: var(--muted-color);
  }

  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .button-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
  }

  .button-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.7rem 1rem;
    border: 1px solid var(--border-color);
    border-radius: 999px;
    color: var(--text-color);
    text-decoration: none;
    background: rgba(255, 255, 255, 0.45);
  }

  @media (min-width: 900px) {
    .site-header {
      grid-template-columns: auto 1fr;
      align-items: center;
    }

    .site-nav {
      justify-content: flex-end;
    }
  }
</style>
